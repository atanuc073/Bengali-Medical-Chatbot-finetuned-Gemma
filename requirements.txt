# ── Step 1: Install PyTorch GPU first (run separately) ──
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# ── Step 2: Then install these ──
unsloth
unsloth_zoo
transformers>=4.51.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
datasets>=2.14.0
trl>=0.7.0
peft>=0.6.0
IndicTransToolkit
sentencepiece
protobuf
